[acl_build_options]
input_format="ND"
# OPT full-forward (no cache), fixed seq=96 (infer pads shorter prefixes, mask=0).
input_shape="inputs_embeds:1,96,2560;attention_mask:1,96;position_ids:1,96"

[acl_init_options]
ge.exec.precision_mode = force_fp16

[ascend_context]
plugin_custom_ops=All
