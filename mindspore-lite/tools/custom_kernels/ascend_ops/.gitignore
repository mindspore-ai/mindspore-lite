# Build output (generated by build_all_ops.sh — never commit).
build_out/

# Per-SoC staged output / tarballs produced into an output dir.
*.tar.gz
*.run

# Python bytecode (the build invokes cmake/util/*.py).
__pycache__/
*.pyc
