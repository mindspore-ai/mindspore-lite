# Export toolchain dependencies.
# transformers >= 4.57: the GGUF skeleton path uses the 4.57 `gguf_file=`
# kwarg.  The forward logic lives in standalone NNRT wrappers (models/_base +
# per-model subclasses) that only touch stable public transformers APIs, so
# there is no monkey-patch coupling to a single transformers minor anymore.
torch>=2.0
transformers>=4.57,<5
onnx>=1.16
onnxslim>=0.1.9   # slim(..., skip_fusion_patterns=...) requires >= 0.1.9
numpy

# GGUF Q4_0 weight injection (gguf_loader.py).
gguf==0.17.1
safetensors
tqdm

# Required by transformers for GGUF checkpoint loading.
accelerate
