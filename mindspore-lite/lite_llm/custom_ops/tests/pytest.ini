[pytest]
markers =
    device: requires REMOTE_TARGET, MODEL_RUN_TOOLS_PATH, a built custom operator, and a connected device
    cpusim: requires the Lima DDK VM (or local DDK_PATH); runs ascendebug CPU-simulation precision tests
testpaths = .
