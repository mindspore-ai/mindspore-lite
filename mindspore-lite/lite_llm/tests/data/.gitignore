# golden_v1.msl is deterministically regenerable (gen_golden.py); pytest
# (tests/py/conftest.py) and CMake (tests/CMakeLists.txt) auto-generate it
# when missing, so it stays out of the source tree.
/golden_v1.msl
